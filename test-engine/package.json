{
  "scripts": {
    "deploy": "npx serverless deploy && npm run write-env ",
    "write-env": "@powershell -NoProfile -ExecutionPolicy Unrestricted -Command ./write-env.ps1 -functionName test-engine-service-production-app"
  },
  "devDependencies": {
    "serverless": "^3.22.0",
    "serverless-python-requirements": "^5.4.0",
    "serverless-wsgi": "^3.0.1"
  }
}
